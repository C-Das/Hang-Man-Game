var letter = require("./letter.js");

function Word(wrd){
    this.charac = let;
    this.appear = false;

    this.letterRender = function(charac){
      if(this.charac === false ){
        return "_";
      }else {
        return charac;
      }
    }
}